# Backend environment variables
# Copy to .env and fill in values. Do not commit .env.

# Server
PORT=3001

# Supabase (required for auth and for storing QuickBooks tokens)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# QuickBooks Online (required for Invoices → Create in QuickBooks)
QBO_CLIENT_ID=your_quickbooks_client_id
QBO_CLIENT_SECRET=your_quickbooks_client_secret
QBO_REDIRECT_URI=http://localhost:3001/api/quickbooks/callback
FRONTEND_URL=http://localhost:5173
# Optional: sandbox (default) or production
QBO_ENVIRONMENT=sandbox
