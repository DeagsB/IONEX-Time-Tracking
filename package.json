{
  "name": "ionex-time-tracking",
  "version": "1.0.0",
  "description": "IONEX Time Tracking Software",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  }
}
